name: cardinal_drones_crm
description: A new Flutter CRM PWA
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2

dev_dependencies:
  build_runner: ^2.4.15
  flutter_test:
    sdk: flutter
  mason_cli: ^0.1.2
  #very_good_cli: ^0.25.0

flutter:
  uses-material-design: true
